# YouTube 视频下载工具 — 项目策划案

> 版本：v1.0 草案 ｜ 日期：2026-09-16
> 定位：Windows 桌面端（可平滑扩展到 macOS/Linux）的图形化 YouTube 下载器
> 技术基底：Electron + yt-dlp + ffmpeg（沿用你已有的 exe 资产与字幕内嵌经验）

---

## 1. 产品概述

### 1.1 目标
做一个**稳定、好用、不卡网络**的桌面下载器，把 yt-dlp 的强大能力封装成普通人也能用的界面，重点解决三个痛点：
1. 命令行门槛高、参数记不住；
2. 字幕下载后无法内嵌进视频（你已验证 ffmpeg 可行）；
3. 国内网络下易超时、易失败、无重试。

### 1.2 用户画像
- **核心用户**：需要离线收藏教程/视频的普通用户，不懂命令行。
- **进阶用户**：开发者/剪辑师，需要批量、指定格式（mp3/m4a）、内嵌字幕。

### 1.3 价值主张
- 一键粘贴链接即下载，清晰度/格式/字幕可视化勾选；
- 字幕**自动下载并内嵌**进视频（ffmpeg 硬字幕或软字幕轨道）；
- 下载队列 + 断点续传 + 失败自动重试，国内网络友好；
- 纯本地运行，数据不上传，历史可管理。

---

## 2. 功能清单（分三期）

### MVP（v1.0 必须）
| 功能 | 说明 |
|---|---|
| 单视频下载 | 粘贴 URL → 解析信息 → 选清晰度/格式 → 下载 |
| 格式选择 | 合并最佳视频+音频（mp4），或仅音频（m4a/mp3） |
| 字幕下载 | 列出可用字幕语言，下载 .srt/.vtt |
| 字幕内嵌 | 用 ffmpeg 把字幕**硬烧录**进视频，支持「无/中文/英文/中英双字幕」 |
| 下载进度 | 实时进度条 + 速度 + 剩余时间（解析 yt-dlp 输出） |
| 本地历史 | 已完成/失败列表，可重新下载、打开文件所在目录 |

### 进阶（v1.1）
- 播放列表 / 频道批量下载（可勾选条目、限制数量）
- 下载队列管理（并行数限制、限速、暂停/继续）
- Cookie 导入（登录态，解锁会员/限制内容）
- 代理设置（HTTP/SOCKS5，解决地域限制）
- 格式预设（一键「最高画质」「仅音频」「手机适配」）

### 高级（v2.0）
- 订阅频道变更检测、定时自动下载
- 内置简易播放器/字幕预览
- 跨平台构建（macOS .dmg / Linux AppImage）
- 自动更新 yt-dlp / ffmpeg 二进制

---

## 3. 技术方案

### 3.1 核心引擎：yt-dlp
- 原因：活跃维护、支持站点广、字幕/播放列表/格式选择能力完整，比手写解析稳妥。
- 形态：**不要求用户自带**，应用内置 `yt-dlp.exe`（你已有），并通过自动更新保持最新。

### 3.2 GUI：Electron
- 沿用你的 RemindMe 技术栈，降低维护成本。
- 前端框架已确认：**Vue 3 + Vite**（轻、快，构建链路成熟）。
- 主进程负责 spawn yt-dlp 子进程，通过 `child.stdout` 解析进度，经 IPC 推送给渲染进程。

### 3.3 转码与字幕：ffmpeg
- 内置 `ffmpeg.exe`（你已有）。
- **字幕策略（已确认）**：默认 **硬字幕（烧录）**，兼容性最好、任意播放器都能看；可选「不需要字幕 / 仅中文 / 仅英文 / 中英双字幕」。**不做软字幕轨道方案**（简化实现、避免容器兼容坑）。
- 内嵌流程：先 `yt-dlp` 分别下视频+音频+字幕，再 `ffmpeg` 合并烧录。
  - 单语硬字幕：`ffmpeg -i v.mp4 -vf "subtitles=zh.srt" out.mp4`
  - 中英双字幕（中文底行 + 英文顶行）：
    `ffmpeg -i v.mp4 -vf "subtitles=zh.srt:force_style='FontSize=24,PrimaryColour=&H00FFFFFF&',subtitles=en.srt:force_style='FontSize=18,PrimaryColour=&H00CCCCCC&,Alignment=6'" out.mp4`

### 3.4 数据存储
- 下载历史 / 设置：用 `electron-store`（轻量 JSON，无需数据库服务）。
- 二进制资产：`resources/` 目录随包发布 yt-dlp.exe、ffmpeg.exe。

---

## 4. 架构设计

### 4.1 目录结构（草案）
```
youtube-downloader/
├─ main/                 # Electron 主进程
│  ├─ index.js           # 窗口 + IPC 注册
│  ├─ downloader.js      # yt-dlp 封装、进度解析、队列
│  ├─ ffmpeg.js          # 合并/字幕内嵌封装
│  └─ paths.js           # 二进制与输出目录管理
├─ renderer/             # Vue 前端
│  ├─ App.vue
│  ├─ views/Download.vue # 主下载界面
│  ├─ views/History.vue  # 历史记录
│  └─ components/        # 进度条、格式选择器等
├─ resources/
│  ├─ yt-dlp.exe
│  └─ ffmpeg.exe
├─ package.json
└─ electron-builder.yml
```

### 4.2 模块关系
```
URL 输入 → 解析(yt-dlp --dump-json)
        → 渲染格式/字幕列表
        → 用户选择 → 加入队列
        → downloader 执行 yt-dlp → 进度 IPC → UI
        → (可选) ffmpeg 合并/内嵌
        → 写入历史(electron-store)
```

### 4.3 关键数据流
- 进度解析：yt-dlp 输出形如 `[download]  45.2% of ... at ... ETA ...`，正则提取百分比/速度/ETA。
- 失败重试：捕获非零退出码，指数退避重试（最多 3 次），超时单独提示「网络/代理」。

---

## 5. 关键流程

### 5.1 下载主流程
1. 用户输入 URL，点「解析」→ 主进程调 `yt-dlp --dump-json --no-warnings`。
2. 解析返回标题、时长、可选格式/字幕列表 → 渲染选择面板。
3. 用户确认 → 生成下载任务（输出模板含标题+格式+字幕标记）。
4. 执行 `yt-dlp -f <fmt> -o <path>`；实时回传进度。
5. 如需内嵌字幕 → 调 ffmpeg 合并 → 产出最终文件。
6. 完成写入历史，提供「打开文件夹」。

### 5.2 字幕内嵌流程（已验证可行，默认硬字幕）
- 下载阶段用 `--write-subs --sub-lang zh-Hans,en` 拿到中文 + 英文字幕（.srt/.vtt）。
- 用户选「不需要字幕」→ 直接合并音视频，不做烧录。
- 选「中文 / 英文 / 中英双字幕」→ ffmpeg 用 `subtitles` 滤镜把字幕**烧录**进画面（不可逆，但任意播放器通用）。
- 中英双字幕：中文主字幕放底部、英文字幕放顶部（Alignment=6），用 `force_style` 调字号/颜色区分。

---

## 6. UI/UX 草案

### 6.1 主界面（三区）
- 顶部：URL 输入框 + 「解析」「下载」按钮 + 设置入口。
- 中部：解析结果卡片（缩略图、标题、时长）+ 格式下拉 + **字幕选项（无 / 中文 / 英文 / 中英双字幕，默认「无」）**。
- 底部：下载队列列表，每行显示进度条/速度/状态/操作（暂停、重试、打开）。

### 6.2 交互要点
- 粘贴即自动解析（防抖 300ms）；
- 默认值：最高可用 mp4 + 无字幕，降低新手门槛；
- 错误态明确区分「链接失效 / 网络超时 / 需登录(cookie)」。

---

## 7. 打包与发布（沿用 RemindMe 方案）

- **平台范围（已确认）**：**仅 Windows**（首版不做 macOS/Linux 跨平台构建）。
- 构建：**electron-builder**，输出 Windows 安装包（NSIS）/ 便携版（portable）。
- 镜像加速：设置 `ELECTRON_BUILDER_BINARIES_MIRROR` 走 npmmirror，缓解 GitHub 443 不稳定。
- 二进制随包：`extraResources` 把 yt-dlp.exe、ffmpeg.exe 打进 `resources/`。
- 发布闭环（与 RemindMe 一致）：`commit → tag → push` 一次性推送，GitHub Releases 附安装包。
- 自动更新：后续接入 electron-updater，先手动发版。

---

## 8. 开发路线图（里程碑）

| 阶段 | 目标 | 交付 |
|---|---|---|
| M1 | 工程脚手架 + 窗口 + URL 解析 | 能解析出视频信息 |
| M2 | 单视频下载 + 进度 + 历史 | 可下载一个 mp4 |
| M3 | 格式/音频选择 + 字幕下载 | 可选清晰度/仅音频/字幕 |
| M4 | ffmpeg 合并与字幕内嵌 | 产出带字幕的最终视频 |
| M5 | 打包 + 镜像 + 发布流程 | 可安装 exe |
| v1.0 | 收尾、回归测试、README | 稳定首版 |

---

## 9. 合规与风险

- **服务条款**：YouTube ToS 原则上禁止批量下载，工具定位为「个人离线收藏/合理使用」，不内置破解、不提供批量爬取整站的无脑开关（播放列表下载需用户主动选择）。
- **版权**：界面提示「仅下载你有权使用的内容」，不提供规避付费墙的默认能力（Cookie 为高级可选项，用户自担风险）。
- **网络**：国内访问 YouTube 本就受限，工具需明确「依赖用户自身网络/代理」，不捆绑任何翻墙能力。
- **二进制维护**：yt-dlp 频繁更新以应对站点变动，需设计「检查更新」机制，否则易失效。

---

## 10. 技术难点与对策

| 难点 | 对策 |
|---|---|
| yt-dlp 进度输出不稳定 | 统一用 `--newline --progress-template` 结构化输出，正则 + 容错 |
| 合并音视频编解码兼容 | 默认输出 mp4（H.264+AAC）；字幕硬烧录进 mp4，避免容器/播放器兼容坑 |
| Windows 下 spawn yt-dlp 路径/编码 | 用 `path` 解析绝对路径，子进程 `shell:false`，输出 `utf8` |
| GitHub 443 导致更新/依赖失败 | npmmirror 镜像 + 重试 + 用户可手动替换 exe |
| 大文件中断 | 利用 yt-dlp 续传（`-c`），失败重试从上次字节继续 |

---

## 11. 关键决策（已确认）

| 决策项 | 结论 |
|---|---|
| 前端框架 | **Vue 3 + Vite** |
| 平台范围 | **仅 Windows**（首版不做跨平台） |
| 字幕方案 | **默认硬字幕（烧录）**，选项：不需要 / 仅中文 / 仅英文 / 中英双字幕 |
| 本次范围 | **仅出策划案**，不搭工程脚手架（待你确认后再进入 M1） |

> 备注：以上假设建立在你已具备 `yt-dlp.exe` 与 `ffmpeg.exe`、且熟悉 Electron + electron-builder + npmmirror 发布链路的基础上。如某条假设不符，告诉我即可调整。
